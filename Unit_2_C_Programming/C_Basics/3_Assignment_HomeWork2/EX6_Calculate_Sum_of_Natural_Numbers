#include <stdio.h>
#include <stdlib.h>

int main(void) {

	int x;
	int sum=0;
	printf("Enter an integer number: ");
	fflush(stdin); fflush(stdout);
	scanf("%d",&x);

	for(int y=0;y<=x;y++)
	{
		sum=sum+y;
	}

	printf("Sum = %d",sum);

}
