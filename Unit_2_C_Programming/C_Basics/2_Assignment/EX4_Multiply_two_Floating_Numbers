#include <stdio.h>
#include <stdlib.h>

int main(void) {

	float x,y,z;
	printf("Enter two numbers: ");
	fflush(stdin); fflush(stdout);
	scanf("%f %f",&x,&y);
	z=x*y;
	printf("Product: %f",z);
}
