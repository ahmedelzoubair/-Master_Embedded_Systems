/*
 ============================================================================
 Name        : test1.c
 Author      : 
 Version     :
 Copyright   : Your copyright notice
 Description : Hello World in C, Ansi-style
 ============================================================================
 */

#include <stdio.h>
#include <stdlib.h>

int main(void) {

	/*
	char x= ' ';
	printf("a%cd\n",x );
	printf("a%dd",x);

	char x= 128;
	printf("a%cd \n",x );
	printf("a%dd",x);

	int x;
	printf("plz enter numb change ass in ASCII \n");
	fflush(stdout);
	scanf("%d",&x);
	printf("the result = %c",x);
//////////////////////////////////////////////////
	float radius;
	float area ;
	float circumference;
	char choose;

	printf ("plz inter the radius:\n");
	fflush(stdin); fflush(stdout);
	scanf("%f",&radius);

	printf ("if u need to callcoulate area but a if circumference but c \n");
	fflush(stdin); fflush(stdout);
	scanf("%c",&choose);

	if (choose=='a')
	{
		area = 3.14159 * radius * radius;
		printf ("the area is = %f \n" , area);
	}
	else if (choose=='c')
	{
		circumference = 2 * 3.14159 * radius;
		printf ("the circumference is = %f \n" , circumference);
	}
	else
	{
		printf ("wrong choose \n");
	}
///////////////////////////////////////////////
	int firstNum;
	int sacandNum ;
	int thierdNum;

	printf ("plz iner 1st num \n");
	fflush(stdin); fflush(stdout);
	scanf ("%d",&firstNum);

	printf ("plz iner 2nd num \n");
	fflush(stdin); fflush(stdout);
	scanf ("%d",&sacandNum);

	printf ("plz iner 3rd num \n");
	fflush(stdin); fflush(stdout);
	scanf ("%d",&thierdNum);

	if(firstNum > sacandNum && firstNum >thierdNum )
	{
		printf ("the greates is 1st num" );
	}
	else if(sacandNum > firstNum && sacandNum >thierdNum )
	{
		printf ("the greates is 2nd num ");
	}
	else
	{
		printf ("the greates is 3rd num " );
	}

////////////////////////////////////////////
	float radius;
	float area ;
	float circumference;
	char choose;

	printf ("plz inter the radius:\n");
	fflush(stdin); fflush(stdout);
	scanf("%f",&radius);

	printf ("if u need to callcoulate area but a if circumference but c \n");
	fflush(stdin); fflush(stdout);
	scanf("%c",&choose);

	switch (choose)
	{
	case 'a':
	case 'A':
	{
		area = 3.14159 * radius * radius;
		printf ("the area is = %f \n" , area);
	}
	break;

	case 'c':
	{
		circumference = 2 * 3.14159 * radius;
		printf ("the circumference is = %f \n" , circumference);
	}
	break;
	default:
		printf ("wrong choise");
	}

//////////////////////////////////////////
	int x;
	int y;

	for(x=0;x<=9;++x);
	{
		printf("%d : Hellow World \n",x);
	}

	for(y=0,x=1;x<100;x++)
	{
		//y=y+x;
		y += x;
	}
	printf("summation of values between (1 and 99) is %d",y);


	int studNum;
	int degree;
	int sum=0;
	float avg;

	printf("plz inter num of students \n");
	fflush(stdin); fflush(stdout);
	scanf("%d",&studNum);

	for(int x=1;x<=studNum;x++)
	{
		printf("Enter student (%d) degree :",x);
		fflush(stdin); fflush(stdout);
		scanf("%d",&degree);
		sum= sum+degree;
	}
	avg = sum / studNum;
	printf("Avg stud degree is : %f ",avg);
	//printf("Avg stud degree is : %f ",sum/studNum);



	int x=1;
	int y=0;
	int studnum;
	float avg,degree;
	float sum=0;

	while(x<9)
	{
		y=y+x;
		x++;
	}
	printf("the sum = %d \n",y);

	x=1;
	printf("enter studnum: ");
	fflush(stdin); fflush(stdout);
	scanf("%d",&studnum);

	while(x<=studnum)
	{
		printf("enter degree stud %d: ",x);
		fflush(stdin); fflush(stdout);
		scanf("%f",&degree);
		sum=sum+degree;
		x++;
	}
	avg=sum/studnum;
	printf("the avg is %f",avg);
	*/

	int x;
	int y;

	for(y=0;y<=9;y++)
	{
		for(x=y;x<=9;x++)
		{
			printf("%d ",x);

		}

		printf("\n");
	}
}
